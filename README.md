# Webserver installing Script

**This Skript installs a simple webserver with mysql db, that is connectet to phpmyadmin. On top there is the possibility to make it with a domain and an ssl certificate.**
